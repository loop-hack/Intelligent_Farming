{
  "name": "Mosquitto Bridge",
  "connectionType": "mqtt",
  "connectionStatus": "open",
  "uri": "tcp://mosquitto:1883",
  "sources": [{
    "addresses": ["intelligent_farm/sensors/soil_1"],
    "qos": 0,
    "authorizationContext": ["nginx:ditto"],
    "enforcement": {
      "input": "org.intelligent_farm:soil_sensor_1",
      "rule": "{{ input }} == {{ thing:id }}"
    },
    "payloadMapping": ["javascript"]
  }],
  "mappingDefinitions": {
    "javascript": {
      "mappingEngine": "JavaScript",
      "options": {
        "incomingScript": "function mapToDittoProtocolMsg(headers, textPayload) { var data = JSON.parse(textPayload); return Ditto.buildDittoProtocolMsg('org.intelligent_farm', 'soil_sensor_1', 'things', 'twin', 'commands', 'modify', data.path, headers, data.value); }"
      }
    }
  }
}